# DemoRestaurant

How to run:
1. Run: `ng serve`
2. Open `http://localhost:4200/` on browser

How to build:
- Run: `ng build`

How to run unit test:
- Run: `ng test`

How to run e2e test:
- Run: `ng e2e`
